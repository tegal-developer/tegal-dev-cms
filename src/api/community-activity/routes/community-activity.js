'use strict';

/**
 * community-activity router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::community-activity.community-activity');
