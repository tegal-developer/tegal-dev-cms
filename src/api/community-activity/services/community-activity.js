'use strict';

/**
 * community-activity service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::community-activity.community-activity');
