'use strict';

/**
 * event-challenge-participant router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::event-challenge-participant.event-challenge-participant');
