'use strict';

/**
 * community-benefit controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::community-benefit.community-benefit');
