'use strict';

/**
 * challenge-participant router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::challenge-participant.challenge-participant');
