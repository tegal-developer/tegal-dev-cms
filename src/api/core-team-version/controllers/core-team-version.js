'use strict';

/**
 * core-team-version controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::core-team-version.core-team-version');
